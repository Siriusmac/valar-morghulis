// Copy this file to Secrets.xcconfig and replace the example values locally.
// Do not commit Secrets.xcconfig.
// The empty $() keeps // inside URL values from being parsed as a comment.

SUPABASE_URL = https:/$()/example-project.supabase.co
SUPABASE_PUBLISHABLE_KEY = example-publishable-key
